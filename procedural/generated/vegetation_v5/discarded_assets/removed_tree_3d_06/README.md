# Removed tree_3d_06 assets

This recoverable archive contains every `tree_3d_06` production variant that
was removed from the active V5 catalog:

- `trees/`: 10 GLBs (five colors across LOD0 and LOD1).
- `review/`: two green audit renders, one per LOD.
- `manifest_before_removal.json`: the 120-asset manifest captured before the
  removal.

None of these files belongs to the active production manifest.
